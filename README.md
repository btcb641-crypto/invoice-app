# 🧾 تطبيق نظام الفواتير - Flutter

تطبيق إدارة الفواتير المبني بـ Flutter مع WebView.

## 📋 المتطلبات

- Flutter SDK 3.0.0 أو أحدث
- Android Studio أو VS Code
- Java JDK 11 أو أحدث

## 🚀 خطوات التشغيل

### 1️⃣ تثبيت Flutter
```bash
# تحقق من التثبيت
flutter doctor
```

### 2️⃣ استنساخ المشروع
```bash
cd flutter_invoice_app
```

### 3️⃣ تثبيت التبعيات
```bash
flutter pub get
```

### 4️⃣ تشغيل التطبيق
```bash
# للتشغيل على المحاكي
flutter run

# لبناء APK
flutter build apk --release
```

## 📱 بناء ملف APK

```bash
# APK للاختبار
flutter build apk --debug

# APK للنشر
flutter build apk --release

# ستجد الملف في:
# build/app/outputs/flutter-apk/app-release.apk
```

## 📂 هيكل المشروع

```
flutter_invoice_app/
├── lib/
│   └── main.dart           # الكود الرئيسي
├── assets/
│   └── web/
│       └── index.html      # صفحة الويب
├── android/
│   └── app/
│       └── src/main/
│           └── AndroidManifest.xml
├── pubspec.yaml            # التبعيات
└── README.md
```

## ✨ المميزات

- ✅ إدارة العملاء
- ✅ إدارة المنتجات
- ✅ إنشاء الفواتير
- ✅ حفظ الفواتير
- ✅ طباعة وتصدير PDF
- ✅ نسخ احتياطي
- ✅ يعمل بدون إنترنت

## 🛠️ حل المشاكل

### إذا واجهت مشكلة في Gradle:
```bash
cd android
./gradlew clean
cd ..
flutter clean
flutter pub get
```

### إذا واجهت مشكلة في التبعيات:
```bash
flutter pub upgrade
flutter pub get
```

### إذا لم تعمل صلاحيات التخزين:
1. اذهب إلى إعدادات الهاتف
2. ابحث عن التطبيق "نظام الفواتير"
3. اضغط على "الأذونات" أو "Permissions"
4. فعّل "التخزين" و "الملفات والوسائط"

## 📱 الصلاحيات المطلوبة:

| الصلاحية | الاستخدام |
|----------|-----------|
| **الإنترنت** | تحميل الخطوط والمكتبات |
| **التخزين** | حفظ ملفات PDF والفواتير |
| **الوسائط** | الوصول للصور والملفات |
| **الكاميرا** | (اختياري) لإضافة صور مستقبلاً |

## 📄 الترخيص

MIT License
